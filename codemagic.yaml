workflows:
  ios-workflow:
    name: iOS Compile Check
    instance_type: mac_mini_m2
    max_build_duration: 45

    environment:
      flutter: stable
      xcode: latest
      cocoapods: default

    scripts:
      - name: Get Flutter packages
        script: flutter pub get

      - name: Install pods
        script: |
          find . -name "Podfile" -execdir pod install \;
          echo "========== HAISHINKIT VERSION =========="
          grep -n "HaishinKit" ios/Podfile.lock || echo "HaishinKit NOT FOUND"
          echo "========================================"

      - name: Compile check (prints Swift errors)
        script: |
          set +e
          cd ios
          xcodebuild \
            -workspace Runner.xcworkspace \
            -scheme Runner \
            -configuration Release \
            -sdk iphoneos \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            build 2>&1 | tee /tmp/compile.log
          echo ""
          echo "########## SWIFT ERRORS ##########"
          grep -n "error:" /tmp/compile.log | head -80 || echo "NO ERRORS FOUND"
          echo "########## END ERRORS ##########"
          exit 0

    artifacts:
      - /tmp/compile.log